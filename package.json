{
  "name": "glyphjet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GlyphJet — instant emoji, symbols and kaomoji launcher for Windows",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "node scripts/bench-search.mjs",
    "build:taxonomy": "py -3.13 scripts/build_taxonomy.py",
    "sync:data": "py -3.13 scripts/sync_data.py"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-clipboard-manager": "^2",
    "@tauri-apps/plugin-global-shortcut": "^2",
    "@tauri-apps/plugin-store": "^2",
    "flexsearch": "^0.7.43"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
